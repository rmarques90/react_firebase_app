## Projeto de aplicação reactjs com firebase;

Para rodar editar o arquivo '/utils/firebase.js', 'npm install' e depois rodar o start.

Aplicação possui implementação do módulo FireStorage e Authentication;